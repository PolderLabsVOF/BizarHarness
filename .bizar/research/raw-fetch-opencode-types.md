# opencode SDK Types (types.gen.ts)

## Source: https://github.com/anomalyco/opencode/blob/dev/packages/sdk/js/src/gen/types.gen.ts
- Auto-generated by `@hey-api/openapi-ts` from server OpenAPI 3.1 spec
- 3907 lines, all `export type` (no interfaces)
- Naming convention: PascalCase for types, camelCase for fields

### Core types

**Session** — `export type Session = { id, projectID, directory, parentID?, summary?: { additions, deletions, ... } }`

**Message** — `export type Message = UserMessage | AssistantMessage`
- UserMessage: `{ id, sessionID, role: "user", time: { created }, summary?, agent, model: { providerID, modelID }, system?, tools? }`
- AssistantMessage: `{ id, sessionID, role: "assistant", time: { created, started?, finished? }, model, parts[], error?, ... }`

**Part** — `export type Part = TextPart | { id, sessionID, messageID, ... }`
- TextPart: `{ type: "text", text, toolCalls?, ... }`
- Also: ReasoningPart, FilePart, ToolStatePending, etc.

### Discriminated event types (all follow same pattern)

```ts
export type EventServerConnected = {
  type: "server.connected"
  properties: { [key: string]: unknown }
}

export type EventServerInstanceDisposed = {
  type: "server.instance.disposed"
  properties: { directory: string }
}

export type EventInstallationUpdated = {
  type: "installation.updated"
  properties: { version: string }
}

export type EventLspClientDiagnostics = {
  type: "lsp.client.diagnostics"
  properties: { serverID: string, path: string }
}
```

Event type strings are dot-separated: `"server.connected"`, `"installation.updated"`, `"lsp.client.diagnostics"`, etc.

### Error types (discriminated by `name`)
- `ProviderAuthError` — `{ name: "ProviderAuthError", data: { providerID, message } }`
- `UnknownError` — `{ name: "UnknownError", data: { message } }`
- `MessageOutputLengthError` — `{ name: "MessageOutputLengthError", data: {...} }`
- `MessageAbortedError` — `{ name: "MessageAbortedError", data: { message } }`
- `APIError` — `{ name: "APIError", data: { message, statusCode?, isRetryable, responseHeaders?, responseBody? } }`
- `StructuredOutputError` (from SDK docs)

### Other types
- `FileDiff` — `{ file, before, after, additions, deletions }`
- TextPart types: `TextPart`, `ReasoningPart`, `FilePart`, etc.
- `FileSource | SymbolSource` for file parts
- Session summary with diffs
