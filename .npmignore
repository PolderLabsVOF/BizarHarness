# Exclude test files from npm tarball
cli/*.test.mjs

# Internal design docs (not for npm)
.bizar/

# Project plans (user data, not package)
plans/

# GitHub wiki (lives on GitHub)
wiki/

# Standard npmignore
node_modules/
*.log
.DS_Store

# Self-hosted frontend assets — must ship in the npm tarball
# so the local plan server can serve them with no network access.
!templates/plan/htmx.min.js
