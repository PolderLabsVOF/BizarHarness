---
description: Run the canonical Bizar deep-interview Socratic pre-build clarification loop and crystallize a spec artifact.
argument-hint: "[--quick|--standard|--deep] <broad or ambiguous request>"
disable-model-invocation: true
allowed-tools: Skill
---

Invoke the Skill tool exactly once. For `/bizar-harness:deep-interview`, select the installed `bizar-harness:deep-interview` skill; for project `/deep-interview`, select the installed `deep-interview` skill. Pass `$ARGUMENTS` unchanged. Do not invoke this command again or reimplement the skill.
