---
description: Run Bizar's native multi-lane migration workflow (one contract owner, per-wave barriers, parallel module consumers).
argument-hint: "--from <old> --to <new>"
disable-model-invocation: true
allowed-tools: Skill
---

Invoke the installed `bizar-migrate` skill exactly once and pass `$ARGUMENTS` unchanged. Do not invoke this command again or reimplement its workflow.
