---
description: Run Bizar's native multi-dimensional security audit (STRIDE threat model plus authz/secret/supply-chain/runtime lenses with verifier-of-finder refutation).
argument-hint: "[--fix] <target>"
disable-model-invocation: true
allowed-tools: Skill
---

Invoke the installed `bizar-sec-audit` skill exactly once and pass `$ARGUMENTS` unchanged. Do not invoke this command again or reimplement its workflow.
