---
description: Show the current statusline configuration
allowed-tools: ["Bash"]
---

# /statusline

Show the current statusline configuration:

```bash
bizar statusline show
```

This displays:
- Where the settings are stored
- Which template is active
- The current statusLine configuration

Use `/statusline` to quickly check if the statusline is installed and what it looks like.
