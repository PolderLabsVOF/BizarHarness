---
description: Run the canonical Bizar Ultragoal multi-objective tracker with a four-lane completion fence.
argument-hint: "[--mode aggregate|per-story] <objective text or resumable run id>"
disable-model-invocation: true
allowed-tools: Skill
---

Invoke the Skill tool exactly once. For `/bizar-harness:ultragoal`, select the installed `bizar-harness:ultragoal` skill; for project `/ultragoal`, select the installed `ultragoal` skill. Pass `$ARGUMENTS` unchanged. Do not invoke this command again or reimplement the skill.
