---
description: Run Bizar's native major-version dependency upgrade workflow for one package.
argument-hint: "<package>@<new-major>"
disable-model-invocation: true
allowed-tools: Skill
---

Invoke the installed `bizar-upgrade` skill exactly once and pass `$ARGUMENTS` unchanged. Do not invoke this command again or reimplement its workflow.
